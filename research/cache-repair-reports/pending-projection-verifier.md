PASS after the integrated fallback and fixture corrections; the authoritative re-verification is `pending-projection-fullsuite-review.md`.
Verified base SHA: `dc8f07458aad1db2d7fe3940643b23732cba6876`; target is its uncommitted three-file diff on `work/cache-pending-input` (diff SHA-256 `fb4034ff78c5cfd064646b06525e77faa705c9352e527dda101e801868432c28`).
Commands/evidence: `git diff --check` exited 0; scoped diff is 204 insertions/10 deletions, with 38 inserted/10 deleted production lines and 166 test lines.
Targeted red: `wake-and-compaction-threshold.log` ran both added tests twice; incoming-input projection failed with 2 requests versus 3, and usage-less-tail accounting failed with 3 versus 4.
Targeted green: `usage-and-pending-green.log:126-130` records both added tests PASS and `110 tests run: 110 passed, 4082 skipped` in the shared integration checkout.
Green applicability: `context_window.rs` is byte-identical between the reviewed and integration worktrees; the pending-projection hunks in `turn.rs` are identical (integration adds only the separately reviewed `Arc<StepContext>` compaction-parity hunk); both added test functions have identical SHA-256 values in both worktrees.
Criteria → proof: queued input affects the pre-turn decision at `core/src/session/turn.rs:1087-1114`; stored history remains the compact input and pending text appears once afterward at `core/tests/suite/compact.rs:4907-4937`; total/body-after-prefix, fallback-buffer, and hard-window policy remain at `core/src/session/context_window.rs:67-120`.
Findings: none that block this first slice.

## Projection and accounting review

- `run_pre_sampling_compact` receives the still-unrecorded `&[TurnInput]`, estimates every current enum variant with an exhaustive `match`, and combines multiple items with `i64::saturating_add` (`core/src/session/turn.rs:1087-1108`). A future `TurnInput` variant cannot silently escape this projection because the match has no wildcard arm.
- User input is converted through the same `Session::response_item_from_user_input` path used when it is later recorded (`core/src/session/turn.rs:1099-1101`; `core/src/session/mod.rs:3323-3356,4554-4576`). Inter-agent communication likewise uses the same `to_model_input_item` conversion as recording (`turn.rs:1104-1106`; `session/mod.rs:3670-3688`). Function-call outputs and annotated response items already carry their model item, and envelope metadata is correctly excluded from the token estimate (`turn.rs:1102-1103`; `hook_runtime.rs:700-752`).
- There is no double count on the normal path. Pre-turn projection runs before current input is recorded (`turn.rs:177-194` versus `287-293`), and the status calculation adds pending tokens once to `sess.get_total_token_usage()` (`context_window.rs:67-68`). Existing status consumers pass zero (`context_window.rs:25-29,46-58`). For `BodyAfterPrefix`, the corrected fallback uses active tokens when no prefill exists; after a prefill exists, the projected pending input remains included in post-prefix growth (`context_window.rs:78-89`).
- The change leaves the compaction model and request builder unchanged. Previous-model downshift compaction still runs first; the threshold is then evaluated using the current turn's model/config, and the existing pre-turn `run_auto_compact` route captures its step as before (`turn.rs:1094-1095,1109-1130`). `Total` versus `BodyAfterPrefix`, the full context-window cap, fallback buffer, and `>=` trigger remain unchanged (`context_window.rs:70-120`).
- Projection is read-only: the conversion and estimator calls construct temporary `ResponseItem`s and serialize/measure them. History mutation still occurs only after compaction through the established hook/record path. The first regression independently proves the compact request excludes the pending message and the subsequent sampling request contains it exactly once (`compact.rs:4914-4937`). No context fragment, rollout record, protocol field, or public crate API was added.
- Raw `ResponseItem`/function-output estimates can be conservative because later history recording may filter non-API items or truncate oversized tool output (`context_manager/history.rs:349-387,771-794`). Current-turn prompt hooks can also reject a user item after it was projected (`hook_runtime.rs:661-698`; `turn.rs:287-293`). These cases can cause an early compaction, not an oversized request or lost/duplicated input. They do not invalidate the safety-oriented first slice; exact prepared-history projection would require moving or duplicating later record-time policy and is beyond the requested surgical change.

## Pareto boundary

The remaining current-turn world-state/context-update, skill/plugin injection, and user-prompt-hook additional context are material model-visible inputs recorded after this decision (`core/src/session/turn.rs:230-315`), so this patch does not complete the original broad TODO. Prior-turn async hook results are drained before projection and are already included in stored usage (`turn.rs:171-174`). The repository mechanism inventory explicitly says user input is the smallest useful first commit and permits context diffs/full reinjection as a second small commit (`research/cache-repair-reports/compaction-tools-inventory.md:56`). I therefore adjudicate the omission as an intentional bounded first slice within the agreed Pareto scope, not a correctness blocker. Keep the follow-up in the durable cache-repair ledger because the in-source TODO was replaced.

## Test and shape review

- `pre_turn_auto_compact_counts_incoming_user_input_without_compacting_it` has a non-vacuous boundary: stored usage 190 is below the 200 limit, while the independent 800-byte pending suffix crosses it. Its ordered request count, compact-prompt membership, pending-text exclusion, ordinary-follow-up identification, and exactly-once check distinguish projection from premature recording or duplication (`compact.rs:4860-4937`).
- `auto_compact_counts_usage_less_history_before_next_sampling` is correctly a combined-mechanism regression. Its PASS depends on the separately owned accepted-usage/history-boundary fix, not on pending projection alone; the integration green demonstrates the pair together (`compact.rs:4941-5022`; `usage-and-pending-green.log:127-130`).
- The tests use public turn submission and the real history/threshold/local-compaction/request path with only provider I/O mocked. The recorded red and restored green satisfy their executed discriminators. The user-input behavior has integration coverage; the other three enum branches are cleared by exact conversion-path source comparison and exhaustive matching rather than separate request-level cases.
- Shape/size is acceptable: no new trait, type, dependency, error variant, compatibility layer, or external API; the new status helper has two production consumers, while all other callers preserve the zero-pending interface. The changed helper is below the repository function thresholds, the production delta is far below 800 lines, and the comment records the ordering invariant rather than restating syntax.

## Graph, limits, and cleanup

Graph project `Users-viktor-Projects-github-codex` was ready. Searches located the threshold functions, estimator, four input variants, record paths, and body-after-prefix coverage; inbound traces found `run_turn` as the sole caller of `run_pre_sampling_compact` and three existing consumers of the zero-pending status API. `check_index_coverage` returned `metadata_match` / `no_recorded_issue` for every relied-on production/test/support file. The graph is best-effort and indexes the root checkout, so modified source and all cited call paths were checked directly in `/private/tmp/codex-cache-pending-input`.

Fixes made: none; this was a read-only review. No build, Cargo target, formatter cache, or temporary artifact was created. The final cleanup check found about 12 GiB free on the shared volume; preserved diagnostic evidence was not removed.

Landing limitations owned by the coordinator: no mutation sweep or commit gate is supplied for this uncommitted unit, and the green run comes from the shared integration checkout rather than this isolated worktree. The file/hunk/function identity checks above make the green relevant to this review; final integration/full-suite/gate evidence remains required before installation.
