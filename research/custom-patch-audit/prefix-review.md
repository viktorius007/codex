STATUS: NO PROVEN DEFECT IN ASSIGNED SLICE
COMMIT: d5f8ef066a86255fc1e84c1b2a9759c222aa9f4d
CHANGED: report.md and findings.json only; no production or test edits
OPEN: No significant candidate survived source tracing to justify a red regression test.
BLOCKERS: No Cargo build allocated; no tests executed in this worktree.
FILES_REVIEWED: core session/mod.rs, turn.rs, turn_context.rs, context_window.rs, rollout_reconstruction.rs, session_prefix.rs, client.rs, client_common.rs, compact.rs, compact_remote_v2.rs, context_manager/history.rs, state/session.rs, agent/role.rs, context/world_state/mod.rs; corresponding changed tests and models-manager/manager.rs at affected seams.
PROVEN_DEFECTS: 0
---

## Slice and patch inventory

Compared `rust-v0.156.1` with the current local tip. The local changes in this slice (a) place fresh-agent role guidance after shared startup context, (b) freeze model presets and role-tool text within a thread, (c) include the active tool catalog in local compaction requests, (d) project incoming input into pre-turn compaction thresholds, and (e) tie token counts to accepted history positions and reconstruct that boundary on resume. `session_prefix.rs` also caps the rendered agent-completion envelope. These are the intended mechanisms visible in the diff, not evidence of a defect.

## Checked paths

- **Fresh startup ordering.** `Session::build_initial_context_with_world_state` in `codex-rs/core/src/session/mod.rs:4318-4409` emits shared developer sections, contextual user sections, the multi-agent usage hint, then the active mode. `MultiAgentUsageHintState::render_diff` in `codex-rs/core/src/context/world_state/multi_agent_usage_hint.rs:39-60` is the world-state source of the empty-marker usage hint. Although the builder stores only one such hint, the checked world-state section supplies one, and extension fragments use `WorldStateContextFragment` with the default separate-message behavior (`context/world_state/mod.rs:143-175`). No dropped reachable fragment was established.
- **Frozen descriptions.** `Session::model_catalog_snapshot` and `spawn_role_spec` in `codex-rs/core/src/session/turn_context.rs:912-960` retain a thread snapshot and reuse the role text while the configured role map matches. The first snapshot uses the in-memory model catalog through `ModelsManager::get_remote_models` and `build_available_models` (`models-manager/src/manager.rs:145-187`). The tool catalog is built from that turn context, so an unrelated manager refresh does not change its description mid-thread. This is a deliberate freshness tradeoff, and no incorrect request was established.
- **Actual outbound seam.** `Session::record_context_updates_and_set_reference_context_item` records initial context or a diff before sampling (`session/mod.rs:4546-4611`). `run_turn` takes `clone_history().for_prompt(...)` after accepted input and world-state updates (`session/turn.rs:490-548`), then `build_prompt` takes the step's ordered `model_visible_specs()` (`session/turn.rs:1609-1627`). `ModelClientSession` formats input and creates `ResponsesApiRequest` from that prompt (`client.rs:995-1110`); `Prompt::get_formatted_input_for_request` normalizes image detail on a copy (`client_common.rs:64-71`). The checked source shows no local patch that silently substitutes a different tool list or base instruction between rebuilt context and the request.
- **Compaction.** Local compaction now sends `step_context.tool_router.model_visible_specs()` and the same session base instructions (`compact.rs:288-312`). Pre-turn threshold projection estimates the incoming turn items before recording them (`session/turn.rs:1308-1349`), while the compaction request still uses prior stored history. The existing integration scenario at `core/tests/suite/compact.rs:5136-5250` checks that the following ordinary request carries the incoming cwd, image and text exactly once. I did not call the absence of projected world-state changes a defect: the inspected path does not establish a concrete, significant extra provider request under the task's proof bar.
- **Usage and resume.** `ContextManager::update_token_info` stores the accepted history length (`context_manager/history.rs:767-779`); later locally recorded items are added from that boundary (`history.rs:809-862`). Rollout replay positions each `TokenUsageRecord` against its reconstructed history (`session/rollout_reconstruction.rs:385-399`) and resets it after replacement or rollback (`rollout_reconstruction.rs:400-449`). Where a legacy rollout lacks a positioned record, `Session::apply_rollout_reconstruction` estimates the reconstructed history (`session/mod.rs:1734-1765`). Existing changed tests cover usage-less responses, replacement, reasoning once, positioned resume usage, and legacy fallback (`context_manager/history_tests.rs`, `session/rollout_reconstruction_tests.rs`).

## Verification limits

No named failing regression test was produced, so `findings.json` is empty. No test command was run: the brief requires build allocation before Cargo work, and this audit found no qualifying candidate to spend that allocation on. Codebase-memory generation changed during the review; both coverage checks reported `no_recorded_issue`/`metadata_match` for the relied-on files, with Rust semantic coverage `semantic_partial` (`binding_oracle_unavailable`, `expanded_calls_unavailable`, `impl_relationships_unavailable`). Call-edge searches therefore did not license negative conclusions; the flow above was checked directly in source. This is a targeted audit of local changed behavior, not a complete proof of all upstream prompt behavior.
