# Harness audit dispatch contract

Baseline: root /Users/viktor/Projects/github/codex at 34d1bd0a11, stable rust-v0.156.1, installed local.7. Mode: production-readiness, one assigned risk seam. User authorizes six audits plus private local-session evidence investigation, then coordinator-selected surgical repairs of proven significant defects. A clean audit is a valid result. No finding quota, nitpicks, general refactoring, or new feature implementation.

Read root AGENTS.md and applicable skills: /Users/Shared/.claude/skills/{hunt-defects,coding-standards,testing-standards,verification-standards,codebase-memory}/SKILL.md, including evidence-core referenced by hunt-defects. User's executed causal-red-test requirement overrides skill guidance that would prevent test-only reproduction edits. Production source remains unchanged in audit phase. Coordinator chooses fixes; fresh builders implement later.

Each agent owns its assigned worktree, test-only reproduction edits in its slice, and report.md plus findings.json at that worktree root. You are not alone in the codebase: do not revert other work, edit root checkout, share an index/build target, or modify another agent's files. Keep production unchanged. If adding a test requires touching a shared test module, keep the smallest test-only diff in your worktree and record the conflict surface. Index your report in your worktree AGENTS.md; root integration/index is coordinator-owned. Do not commit failing tests into the production suite; preserve the exact patch and raw red log for coordinator replay.

Map the bounded slice's entry points, actors, and terminal outcomes before ranking candidates. Read fully the source you rely on. Cover cancellation, partial failure, boundaries with neighboring components. Report all inventoried entry points/files with disposition and limits; a source review is not proof that no defect exists. GitHub issues in /Users/viktor/Projects/github/codex/research/next-audit-targets.md are leads, not authoritative conclusions; locally fixed mechanisms must be excluded. Check current source before assuming a historical defect persists. Open issues already read are durable research; refresh metadata before rereading bodies.

Findings must state actual user consequence, severity, production reachability, contract/oracle, exact named regression, executed compiling failure on unchanged production, and full test patch. Assert exact causal events/state/output; timeouts only guard hangs. Do not weaken existing policy or expected behavior to manufacture red. No live provider tests; any needed live test is routed to coordinator for Luna. Report incidental issues to coordinator with evidence; do not expand into another agent's slice or fix them.

Use just test for Rust. Before starting Cargo compilation, message coordinator with crate/test command and expected target footprint for a build allocation; continue independent source investigation while waiting. Disk, not subagent capacity, limits simultaneous targets. Keep targets isolated and Cargo jobs32/dev incremental enabled. Prior global kache build failed on missing incremental artifacts; command-local RUSTC_WRAPPER= HOST_CC=clang HOST_CXX=clang++ worked. Do not change global config. V8 archive settings are in repo Cargo config. Never kill running Rust commands. Capture raw test logs, let completion events wake you, do not poll for liveness.

Local ~/.codex records may be read for relevant evidence, never altered, pruned, replayed into a live service, or uploaded. Avoid auth/token/config secrets. Read bounded metadata/structured event fields first; raw transcript content only when necessary for the causal question. Reports contain sanitized counts, event IDs/hashes, versions/timestamps and minimal redacted evidence, not private prompts or outputs. No proof should require real user data in regression fixtures. One dedicated sessions agent scans recent records for cross-cutting leads; request a targeted handoff from coordinator if needed rather than duplicating a full-history scan. Source audit agents may inspect narrow relevant records.

Graph handoff: project Users-viktor-Projects-github-codex, root index generation 2026-09-23T12:29:41Z, Rust semantics partial (bindings, expanded calls, impl relationships unavailable). Parent exact-name search cancel_startup/load_rollout_items/try_run_sampling_request/reserve_slot returned seven rows, complete. Coverage for codex-mcp/src/{connection_manager,runtime}.rs, rollout/src/recorder.rs, core/src/{agent/control/residency,session/turn}.rs reported no recorded file gaps and matching metadata. Source fallback already read recorder load_rollout_items1069–1132, retaining decoded items in Vec; no defect conclusion. Other seams have no parent source conclusions. Graph describes root baseline, not new worktree edits; use graph-first discovery and per-file check_index_coverage, then direct worktree source for gaps and any edited tests. Partial graph edges never establish absence.

Report header: STATUS: DONE|FAIL|BLOCKED; COMMIT: baseline SHA; CHANGED: files; OPEN: unresolved count; BLOCKERS: one line or none; PROVEN_DEFECTS: count; REVIEWED: entry-point/file counts; RED_TESTS: executed/failed counts. Body: slice map and coverage, proven findings only, dismissed candidates with evidence, remaining limits, local-session evidence if any, exact commands/log paths, recommended minimal repair considerations. findings.json follows shared findings contract; report no speculative finding as proven. An unexecuted candidate is explicitly pending, not a completed defect.

Send one DONE <absolute report path> when completed; final response bounded to header and significant outcomes. On a build allocation or real blocking question, send one specific message. On write refusal send BLOCKED-WRITE with header and path. End your turn when finished; coordinator reactivates only for necessary follow-up. Do not spawn further agents or clean your worktree; coordinator handles integration and cleanup.
