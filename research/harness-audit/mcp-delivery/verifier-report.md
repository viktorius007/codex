PASS
COMMIT: `17c9b68c519f40c94dd3705445f27e76531831b4`.
COMMAND: `just test -p codex-core -E 'test(interrupted_idle_mcp_startup_reconnects_for_next_turn)'` at this commit: 1 passed, 4,636 skipped.
COMMAND: `just test -p codex-mcp -E 'test(cancelling_startup_does_not_disable_a_ready_client) | test(reconciliation_reuses_an_unchanged_pending_server_without_waiting) | test(list_all_tools_resolves_server_catalogs_concurrently) | test(capture_binding_retains_cached_tools_that_expire_while_waiting_for_another_server)'`: 4 passed, 240 skipped.
COMMAND: `git show --format= --check HEAD` exited 0; `git rev-parse HEAD` matched the commit above.

| Criterion | Proof |
| --- | --- |
| Idle cancellation permits next-turn recovery | Corrected regression at `core/tests/suite/mcp_optional_startup_grace.rs:22-101` observes `Starting`, one held initialize, `Cancelled`, then `(2, true)` for second initialize and tool in the next model request. The same test failed twice on baseline production with `(1, false)` in `baseline-corrected-red.log:116-156`; it passed in my focused run at the verified commit. |
| No immediate reconnect on interrupt | `core/src/session/mod.rs:4895-4901` only marks `McpRefresh` pending; `core/src/session/mcp.rs:172-247,372` claims it during next-turn resolution. `core/src/session/mcp_refresh.rs:21-31,44-55` preserves a claimed invalidation if publication is cancelled. |
| Ready, dormant, and already cancelled clients stay untouched | `codex-mcp/src/connection_manager.rs:141-156` returns false for those states; the caller marks dirty only for true. The ready-client control passed. A previously cancelled connection is excluded from pending reuse at `connection_manager.rs:482-513`. |
| Every pending server is visited | `McpConnectionSet::cancel_startup` at `connection_manager.rs:937-943` uses non-short-circuit `|=`, so it visits every server while accumulating whether any cancellation occurred. The existing concurrent-catalog control passed; this specific loop property is source-proven. |

FINDINGS: none in the verified diff.

The corrected fixture at `mcp_optional_startup_grace.rs:33` disables deferred tool search, matching adjacent fixtures at lines 157 and 285; `namespace_child_tool` inspects the directly advertised namespace in `core/tests/common/responses.rs:436-454`. The initial unfixed fixture gave `(2, false)` in `codex-rs/green-test.log:38-40`; with the corrected fixture, baseline gave `(1, false)` and the repair gave `(2, true)`. This makes the test's two-part oracle sensitive to the recovery mechanism and to the model-visible outcome. It uses synthetic local servers, no host data, and a five-second timeout only as a hung-test guard.

Shape review cleared all five changed files: the four existing cancellation methods propagate one Boolean through an existing API and add no helper, trait, variant, or new module; the changed branches are shallow and below size limits. The public method comment states its return contract. The added integration test covers one behavior without new production test hooks. `git show --format= --check HEAD` found no whitespace errors. Codebase-memory search found the existing cancellation and dirty-refresh methods; inbound Rust traces were incomplete due partial semantic coverage, so direct worktree source established the chain. Coverage check at graph generation `2026-09-23T13:35:54Z` reported no recorded gaps for the five changed files and the cited refresh/client files, with the same Rust semantic caveat.

Limits: The agreed build allocation excluded scoped lint, a full core/workspace suite, and mutation sweep, so this is a PASS for the assigned repair criteria and focused regressions, not a claim about those broader gates. The builder's four optional-startup controls passed with one automatic retry after a five-second load-sensitive guard (`codex-rs/control-green.log:24-42`); I did not repeat that broader set. The existing controls do not directly assert the Boolean return for every branch or a multi-server cancellation; those branches were inspected in source. The retained target is about 20 GiB and the shared volume had 26 GiB free at the final check; coordinator owns its cleanup.
